name: Moodle Plugin CI

on: [push, pull_request]

jobs:
  call-moodle-ci-workflow:
    uses: Opencast-Moodle/moodle-block_opencast/.github/workflows/moodle-ci@master
    with:
      requires-tool-plugin: true
      requires-mod-plugin: true